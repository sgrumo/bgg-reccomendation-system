services:
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: recco_dev
    ports:
      - "5460:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
