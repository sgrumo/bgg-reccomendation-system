info:
  name: Recommender Health
  type: http
  seq: 2

http:
  method: GET
  url: "{{RECOMMENDER_URL}}/health"
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
