info:
  name: Game Recommendations
  type: http
  seq: 3

http:
  method: GET
  url: "{{RECOMMENDER_URL}}/games/13/recommendations"
  params:
    - name: top_n
      value: "10"
      type: query
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
